# /test
1. Run `python smoke_test.py`, report pass/fail per package
2. If a stage script fails, identify root cause — do not silently retry with different params
3. If the fix requires changing a pinned decision in RULES.md: STOP and ask
